name: lab_4

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        cd lab_4/task_1 
        make test
        ./test
        cd ../task_2/
        make test
        ./test
